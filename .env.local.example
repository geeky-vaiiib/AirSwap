# =============================================================================
# AIRSWAP PRODUCTION ENVIRONMENT CONFIGURATION
# ⚠️  CRITICAL SECURITY: Never commit real secrets to version control!
# 📝 Copy this file to .env.local and fill in REAL values
# 🛡️  Run 'npm run security-audit' before production deployment
# =============================================================================

# =============================================================================
# CRITICAL SECURITY VARIABLES (Required for Production)
# =============================================================================

# JWT Authentication Secret
# 🚨 SECURITY RISK: Generate a strong 64+ character random string
# Run: openssl rand -base64 64 (recommended)
# Or: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
# NEVER use default/development values in production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-generate-new-random-string

# MongoDB Database Connection
# Production: Use secure Atlas connection with encryption
# Format: mongodb+srv://user:pass@cluster.mongodb.net/dbname?ssl=true&retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://jonsnow280905_db_user:muggles2025@air-swap.ygxlbue.mongodb.net/?appName=Air-Swap
MONGODB_DB_NAME=airswap_growth

# Sentry Error Tracking (Production Monitoring)
# Get from: https://sentry.io/settings/projects/YOUR_PROJECT/keys/
# Enable for production error tracking and performance monitoring
SENTRY_DSN=https://your-sentry-dsn-here@sentry.io/project-id
NEXT_PUBLIC_SENTRY_DSN=https://your-public-sentry-dsn-here@sentry.io/project-id
SENTRY_AUTH_TOKEN=your-sentry-auth-token-for-releases

# =============================================================================
# OPTIONAL BUT RECOMMENDED SERVICES
# =============================================================================

# IPFS/NFT.Storage API Key (for file uploads)
# Get from: https://nft.storage/
NFT_STORAGE_API_KEY=your_nft_storage_api_key_here

# Google Earth Engine (for satellite data analysis)
# Get from: https://console.developers.google.com/
GOOGLE_EARTH_ENGINE_SERVICE_ACCOUNT=your-service-account@project.iam.gserviceaccount.com
GOOGLE_EARTH_ENGINE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# =============================================================================
# DEVELOPMENT / LOCAL CONFIGURATION
# =============================================================================

# Next.js Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000

# Environment Indicators
NODE_ENV=development

# Demo Mode (set to 'false' in production)
NEXT_PUBLIC_DEMO_MODE=false

# =============================================================================
# SECURITY CHECKLIST BEFORE DEPLOYMENT
# =============================================================================
# ✅ Run: npm run security-audit (should pass with no errors)
# ✅ JWT_SECRET: 64+ characters, random, unique per environment
# ✅ MongoDB: Using secure Atlas connection, not localhost
# ✅ Sentry: Configured for error tracking in production
# ✅ HTTPS: Forced via security headers in next.config.js
# ✅ Rate Limiting: Enabled via middleware.ts
# ✅ TypeScript: Strict mode enabled in tsconfig.json
# ✅ Dependencies: npm audit should show no critical vulnerabilities
# =============================================================================
